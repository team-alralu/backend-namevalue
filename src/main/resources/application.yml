server:
  port: 1111

spring:
  profiles:
    include: oauth2, db, jwt
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true